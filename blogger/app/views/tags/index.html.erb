<h1> All Tags </h1>

<ul id="tags">
  <% @tags.each do |tag| %>
    <li>
      <%= link_to tag.name, tag_path(tag), class: 'article_title' %>
    </li>
  <% end %>
  <br>
</ul>
<%= link_to "Back to articles", articles_path %>
